import { AppBar, Toolbar } from '@mui/material'
import React from 'react'

const HomePage = () => {
  return (
    <div>
      <AppBar>
        <Toolbar>
            Homepage
        </Toolbar>
      </AppBar>
    </div>
  )
}

export default HomePage
